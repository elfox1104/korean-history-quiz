streamlit>=1.35.0
pymupdf>=1.24.0
Pillow>=10.3.0
pandas>=2.2.0
plotly>=5.22.0
numpy>=1.26.0

# OCR(스캔 PDF 업로드)은 로컬 전용 — 무겁고 클라우드 빌드 한도를 넘기므로 배포에서 제외.
# 로컬에서 스캔 PDF를 적재하려면 아래를 설치하세요:
#   pip install easyocr
